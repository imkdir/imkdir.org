PORT=3011
OWNER_SECRET_KEY=replace-with-a-long-random-secret

# Runtime SQLite path used by backend adapter
SQLITE_PATH="./dev.db"

# Prisma CLI uses this for migrations and generate
DATABASE_URL="file:./dev.db"
